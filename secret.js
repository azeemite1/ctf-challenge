// Secret configuration
const config = {
    debug: false,
    apiEndpoint: "https://ctf-challenge.local/api",
    // CTF{external_secret}
    secretKey: "hidden_in_external_file"
};

console.log("External script loaded successfully");
